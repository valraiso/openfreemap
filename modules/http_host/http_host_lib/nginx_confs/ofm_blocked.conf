# GENERATED - do not edit.
# Source list: /data/ofm/http_host/config/blocked_origins.txt
# Regenerated by http_host.py nginx-config / block / unblock;
# an empty fallback is installed at deploy by ssh_lib/nginx.py.

# host part of Origin: scheme://host[:port]
map $http_origin $ofm_origin_host {
    default '';
    ~*^https?://(?<h>[^/:]+) $h;
}

# host part of Referer: scheme://host[:port]/path
map $http_referer $ofm_referer_host {
    default '';
    ~*^https?://(?<h>[^/:]+) $h;
}

map $ofm_origin_host $ofm_blocked_origin {
    hostnames;
    default 0;
__BLOCKED_ENTRIES__
}

map $ofm_referer_host $ofm_blocked_referer {
    hostnames;
    default 0;
__BLOCKED_ENTRIES__
}

map "$ofm_blocked_origin$ofm_blocked_referer" $ofm_blocked {
    default 1;
    '00' 0;
}
